#include "Board.hpp"

Paint::Pixel px;
Paint::Board(int w, int h, bool centered)
{
    for (int x = 0; x < w; x++)
    {
        for (int y = 0; y < h; y++)
        {
            px = {x, y, 255, 255, 255};
            this->board.push_back()
        }
    }
}
